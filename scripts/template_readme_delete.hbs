---
title: Delete {{ ENTITY }} 
order: 6
type: delete
entity: {{ ENTITY }} 
---

### Delete {{ AN }} {{ ENTITY }} 

The `customer.{{ RESOURCE_URL_NAME }}.delete(resource_name)` sets the `status` field of {{ AN }} {{ ENTITY }} to `REMOVED`. Those entities and their metrics will continue to exist, but they will be read-only. Removed entities cannot be re-enabled.



#### Arguments

- **`resource_name`** (_required_): The resource_name of that {{ ENTITY }}


#### Returns

_Nothing_



```javascript

// Deleting the entity

await customer.{{ RESOURCE_URL_NAME }}.delete('{{EXAMPLE_RESOURCE_NAME}}')

```