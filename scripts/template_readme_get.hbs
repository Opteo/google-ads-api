---
title: Get {{ ENTITY }} 
order: 2
type: get
entity: {{ ENTITY }} 
---

### Get {{ AN }} {{ ENTITY }} 

The `customer.{{ RESOURCE_URL_NAME }}.get(resource_name)` method returns the {{ ENTITY }} identified by a resource_name. 

_Note_: This function is heavily rate-limited by Google, so avoid using it in production.


#### Arguments

- **`resource_name`** (_required_): The resource_name of that {{ ENTITY }}


#### Returns

Returns that {{ ENTITY }} as an object.

``` javascript


// Getting the entity
let result = await customer.{{ RESOURCE_URL_NAME }}.get('{{EXAMPLE_RESOURCE_NAME}}')

```

``` javascript

// Example result
({{ EXAMPLE_OBJECT_GET }})

```