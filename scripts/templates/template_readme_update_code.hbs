---
title: Update {{ ENTITY }} 
order: 5.1
type: update_code
entity: {{ ENTITY }} 
---

```javascript

// Updating the entity

const {{ ENTITY_SNAKECASE }} = {
	resource_name : '{{EXAMPLE_RESOURCE_NAME}}', // The resource_name is required
    // ...any other fields that you would like to update
}

// Passing in a single entity to update
const result = await customer.{{ RESOURCE_URL_NAME }}.update({{ ENTITY_SNAKECASE }})

// Passing in an array of entities to update, validating only
const result = await customer.{{ RESOURCE_URL_NAME }}.update([{{ ENTITY_SNAKECASE }}, other_{{ ENTITY_SNAKECASE }}], {
	validate_only : true
})

```

```javascript

// Example result
{
	results : ['{{EXAMPLE_RESOURCE_NAME}}'],
	partial_failure_error : null,
	request: { /* your request object */ }
}

```